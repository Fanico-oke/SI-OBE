{
  "buildCommand": "npm install --include=dev --prefix frontend && npm install --include=dev --prefix backend && npm run build --prefix frontend && cd backend && npx prisma generate",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.ts" }
  ]
}
