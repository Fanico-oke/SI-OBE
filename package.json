{
  "name": "si-obe",
  "version": "1.0.0",
  "description": "Sistem Informasi Outcome-Based Education",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n \"backend,frontend\" -c \"magenta,cyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "node backend/dist/index.js",
    "db:push": "npm run prisma:push --prefix backend",
    "db:seed": "node backend/scripts/seed_correct.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
